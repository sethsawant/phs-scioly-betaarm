#define ROTMIN  150 
#define ROTMAX  385

const int baseHeader = 0; // Sets which shield header the base servo is wired to

//int baseRot() {
  
  //const int baseHeader = 0; // Sets which shield header the base servo is wired to
  
 // while (uint16_t pulselen = ROTMIN; pulselen < ROTMAX; pulselen++) {
  //  pwm.setPWM(baseHeader, 0, pulselen);
    
  //}
//}


